const fs=require('node:fs');const path=require('node:path');const root=process.cwd();const esbuild=require(path.join(root,'node_modules/esbuild-wasm'));const dest=path.resolve(process.env.REPORT_HARNESS_DIR || '../harness');fs.mkdirSync(dest,{recursive:true});
fs.writeFileSync(path.join(root,'tests/harness-entry.tsx'),'import React from "react"; import {createRoot} from "react-dom/client"; import Workspace from "../app/reports/report-authoring-workspace"; createRoot(document.getElementById("root")!).render(<main className="report-authoring-shell"><Workspace storageKey="studio-regression-test" legacyStorageKey={null}/></main>);');
esbuild.build({entryPoints:[path.join(root,'tests/harness-entry.tsx')],outfile:path.join(dest,'app.js'),bundle:true,platform:'browser',format:'iife',jsx:'automatic',define:{'process.env.NODE_ENV':'"development"'}}).then(()=>{const css=fs.readFileSync('app/globals.css','utf8');fs.writeFileSync(path.join(dest,'styles.css'),'*,*::before,*::after{box-sizing:border-box}body{margin:0;font-family:Arial,sans-serif}'+css.slice(css.indexOf('.report-authoring-shell')));fs.writeFileSync(path.join(dest,'index.html'),'<!doctype html><html lang="ko"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><link rel="stylesheet" href="/styles.css"></head><body><div id="root"></div><script src="/app.js"></script></body></html>');fs.copyFileSync('public/daishin-asset-management.png',path.join(dest,'daishin-asset-management.png')); console.log('harness built',dest);});
